namespace Hilvilla.Helpers
{
    public static class ToSelectListItemsHelper
    {
        //public static IEnumerable<SelectListItem> ToSelectListItems(
        //      this IEnumerable<ContactGroup> groups, int selectedId)
        //{
        //    return

        //        groups.OrderBy(group => group.Name)
        //              .Select(group =>
        //                  new SelectListItem
        //                  {
        //                      Selected = (group.Id == selectedId),
        //                      Text = group.Name,
        //                      Value = group.Id.ToString()
        //                  });
        //}
    }
}